prefix_rule(
    pattern = ["./gradlew"],
    decision = "allow",
    justification = "Allow this trusted project to run its Gradle wrapper",
    match = [
        "./gradlew test",
        "./gradlew build",
        "./gradlew clean build",
    ],
)
